# AI 视频模板生成器方案

## 1. 目标

在现有 AI 视频生成页面中增加“从专业视频创建模板”能力，让用户把一个专业视频片段提炼成可复用的镜头模板。

模板不是固定的首尾帧和一句提示词，而是一份可迁移的生成配方：

- 专业视频样片，用于展示效果
- 首帧、中间帧、尾帧，用于理解运动过程
- 结构化运镜规则
- 时间轴和节奏
- 建筑保护约束
- 可替换变量
- 推荐模型、时长、比例和分辨率
- 经过试生成验证的预览结果

## 2. 产品入口

### AI 视频页

在现有“专业视频渲染引擎”标题区增加：

- `从视频创建模板`：主入口
- `模板管理`：查看草稿、个人模板和企业模板

模板库分类调整为：

- 推荐模板
- 我的模板
- 企业模板
- 系统模板

原有“建筑外景运镜”和“首尾帧创意运镜”改为二级分类或筛选条件。

### 独立创建页

创建流程使用独立页面，不使用小弹窗。建议路由：

```text
/projects/:projectId/ai-video/templates/new
```

原因：视频预览、时间轴、关键帧和 AI 分析结果需要较大操作空间，弹窗会导致内容拥挤。

## 3. 创建流程

### 上传样片

用户上传 MP4、MOV 或 WebM 专业视频。

界面内容：

- 视频上传区
- 文件格式和大小说明
- 素材来源说明
- “我确认有权使用该视频创建内部模板”复选框
- 已上传视频的基本信息：时长、比例、分辨率

系统动作：

- 上传到素材库
- 获取视频时长、宽高、帧率和编码信息
- 生成代理预览视频和封面图
- 异步执行场景切分检测

### 截取镜头

用户从视频中选取一个连续镜头，推荐 3-8 秒。

界面内容：

- 大尺寸视频播放器
- 下方缩略图时间轴
- 可拖动的开始和结束手柄
- 检测到的切镜位置
- 精确到 0.1 秒的起止时间输入
- “循环预览选中片段”按钮

校验规则：

- 片段不能短于 2 秒，不能长于 15 秒
- 推荐 3-8 秒
- 片段中检测到明显切镜时给出警告
- 有大面积字幕、水印或黑边时给出警告

### 确认关键帧

系统默认从片段中抽取：

- 首帧：片段开始位置
- 中间帧：片段运动最明显或时间中点位置
- 尾帧：片段结束位置

用户可以在时间轴上重新指定三个位置。

界面内容：

- 首帧、中间帧、尾帧三张并排预览
- 清晰度、字幕、水印、相似度提示
- “重新选择”操作
- 首尾帧对比滑块

中间帧只用于分析，不强制发送给首尾帧视频模型。

### AI 提炼

提示词大师同时读取：

- 选中的短视频片段
- 首帧、中间帧、尾帧
- 用户补充的模板用途
- 当前建筑工程强约束

AI 返回结构化模板配方，而不是只返回一段提示词。

界面采用左右布局：

- 左侧：样片和关键帧
- 右侧：可编辑的分析结果

分析结果包括：

- 模板名称
- 一级分类和标签
- 适用生成模式
- 运镜类型、方向、路径、速度和强度
- 分阶段时间轴
- 允许变化的内容
- 必须保持的内容
- 负向提示词
- 推荐模型和参数
- 泛化后的最终提示词

### 试生成并发布

模板发布前建议至少完成一次试生成。

界面内容：

- 使用样片关键帧验证
- 替换为当前项目首尾帧验证
- 生成结果与原样片并排预览
- 模板名称、封面、描述和可见范围
- `保存草稿`、`试生成`、`发布模板`

发布范围：

- 仅自己
- 当前企业

只有管理员可以发布为系统模板。

## 4. 模板应用逻辑

用户在 AI 视频页选择模板后，系统按以下优先级构建最终任务：

1. 工程硬约束和安全规则
2. 用户首尾帧中识别出的真实主体与空间关系
3. 用户本次补充要求
4. 模板的运镜配方和时间轴
5. 对应视频模型的格式适配

模板中的具体项目内容必须被泛化。例如：

```text
原样片：镜头环绕上海某玻璃写字楼
模板配方：镜头围绕当前建筑主体缓慢顺时针环绕
```

系统不得把原样片中的建筑类型、项目名称、Logo 或文字带入新任务。

## 5. 结构化模板配方

建议在模板中保存以下 JSON：

```json
{
  "recipe_version": 1,
  "category": "建筑外景运镜",
  "tags": ["环绕", "鸟瞰", "稳定展示"],
  "generation_modes": ["image_to_video", "first_last_frame_video"],
  "camera": {
    "type": "orbit",
    "direction": "clockwise",
    "path": "arc",
    "speed": "slow",
    "intensity": "low"
  },
  "timeline": [
    {
      "from": 0,
      "to": 20,
      "instruction": "保持首帧构图，镜头开始缓慢移动"
    },
    {
      "from": 20,
      "to": 80,
      "instruction": "围绕建筑主体稳定环绕，保持建筑体量和立面关系"
    },
    {
      "from": 80,
      "to": 100,
      "instruction": "镜头减速并自然到达尾帧构图"
    }
  ],
  "preserve": [
    "建筑主体数量、体量、轮廓和层数",
    "道路、主入口和主要设备位置"
  ],
  "allow_change": [
    "轻微光影变化",
    "树木、云层、人物和车辆的自然微动"
  ],
  "negative": [
    "禁止建筑扭曲",
    "禁止新增或删除楼层",
    "禁止镜头抖动和突然切换"
  ],
  "recommended": {
    "duration": 5,
    "aspect_ratio": "adaptive",
    "resolution": "720p"
  }
}
```

## 6. 数据模型

### 扩展 `video_generation_templates`

建议增加：

- `category`：一级分类
- `tags`：检索标签
- `prompt_recipe`：结构化模板配方 JSON
- `preview_asset_id`：试生成预览视频
- `cover_asset_id`：模板封面
- `scope`：personal、organization、system
- `status`：draft、testing、published、archived
- `recipe_version`：配方版本
- `usage_count`：使用次数，后续用于排序

保留现有字段，兼容当前固定模板。

### 新增 `video_generation_template_sources`

用于保存模板的样片来源：

- `template_id`
- `source_video_asset_id`
- `clip_start_ms`
- `clip_end_ms`
- `first_frame_asset_id`
- `middle_frame_asset_id`
- `last_frame_asset_id`
- `analysis_snapshot`
- `license_confirmed`
- `created_by`

独立来源表可以让一个模板未来绑定多个优秀样片，而不污染模板主表。

## 7. 后端服务

### 视频处理

复用项目已有 FFmpeg 能力，增加：

- 视频元数据读取
- 代理预览生成
- 场景切分检测
- 指定时间点抽帧
- 选中片段裁剪
- OCR 和黑边检测

### 提示词大师

现有提示词大师从“输出文本”升级为“输出结构化配方和最终提示词”。

需要增加：

- 视频片段和三张关键帧输入
- JSON Schema 校验
- 项目专有名词泛化
- 建筑结构冲突检测
- 模板风险警告
- 针对不同 Provider 的提示词编译器

### 异步任务

以下动作使用异步任务：

- 视频转码
- 场景检测
- 关键帧提取
- AI 分析
- 试生成

前端显示明确状态：排队中、处理中、需要确认、失败、完成。

## 8. API 设计

建议增加：

```text
POST   /projects/{projectId}/ai-video/template-drafts
GET    /projects/{projectId}/ai-video/template-drafts/{draftId}
POST   /projects/{projectId}/ai-video/template-drafts/{draftId}/clip
POST   /projects/{projectId}/ai-video/template-drafts/{draftId}/frames
POST   /projects/{projectId}/ai-video/template-drafts/{draftId}/analyze
PATCH  /projects/{projectId}/ai-video/template-drafts/{draftId}/recipe
POST   /projects/{projectId}/ai-video/template-drafts/{draftId}/preview
POST   /projects/{projectId}/ai-video/template-drafts/{draftId}/publish
```

现有模板接口继续用于普通模板读取、编辑和删除。

## 9. UI 状态设计

每个步骤都需要以下状态：

- 空状态：告诉用户下一步做什么
- 上传或分析中：显示骨架和当前处理项
- 校验警告：允许返回修改，不直接丢失数据
- 可恢复失败：展示失败原因和重试按钮
- 草稿恢复：再次进入时回到上次完成步骤
- 离开保护：存在未保存修改时提醒

## 10. 实施顺序

### 第一批：可用闭环

- 新建模板创建页和入口
- 专业视频上传
- 手动选择片段
- 首中尾帧提取和调整
- 提示词大师生成结构化配方
- 保存为草稿或个人模板
- 动态模板封面和预览素材

### 第二批：发布闭环

- 试生成
- 企业模板权限
- 模板管理页
- 模板版本和回滚
- 使用模板时进行兼容性检查

### 第三批：智能化

- 自动场景切分和推荐镜头
- OCR、水印和质量风险识别
- 自动泛化项目专有内容
- 按使用次数、成功率和收藏排序
- 一个视频批量提炼多个模板

## 11. MVP 验收标准

- 用户能从 AI 视频页进入创建流程
- 能上传一个专业视频并截取 2-15 秒片段
- 能自动生成并手动调整首中尾帧
- 提示词大师返回可编辑结构化配方
- 配方中不残留原项目名称、Logo 和具体地址
- 能保存草稿并继续编辑
- 发布后模板出现在“我的模板”中
- 选择模板后能自动填充模式、提示词、时长、比例、分辨率和建筑约束
- 每次生成任务保存完整模板配方快照
- 原有系统模板和生成流程保持兼容

## 12. 本次 UI 决策

- 采用独立五段式创建页，不使用弹窗
- 保留现有蓝色主色、灰白背景和 Ant Design 操作习惯
- 步骤导航固定在左侧，主要工作区位于右侧
- 视频裁剪和关键帧确认优先占用画面宽度
- AI 分析结果以表单形式编辑，不让用户直接修改 JSON
- 高级参数默认收起
- 主操作始终位于页面右下区域：继续、试生成、发布模板
- 手机端按单列顺序展示，步骤导航改为顶部横向进度
